{
  "name": "@kentory/auth",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": ". .dev.env && wrangler deploy",
    "dev": "wrangler dev --port 10801"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.1",
    "typescript": "^5.4.2",
    "wrangler": "^3.34.2"
  }
}
