{
  "name": "zgasecauth",
  "version": "0.0.3",
  "private": false,
  "scripts": {
    "deploy": ". .dev.env && wrangler deploy",
    "dev": "wrangler dev --port 10801",
    "test": "./test.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.1",
    "typescript": "^5.4.2",
    "wrangler": "^3.34.2"
  }
}
